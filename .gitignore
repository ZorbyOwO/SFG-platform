# Local editor and agent tooling
.thumbnail
.agents/
uploads/SKILL.md

# Temporary QA captures
qa-*.png
qa-*.jpg

# Generated frontend/backend artifacts and local secrets
node_modules/
web/dist/
web/.vite/
web/.env.local
*.tsbuildinfo
api/.env
api/.runtime/
api/models/

# Commit-pinned biometric model payloads. Acquired and hash-verified by
# biometric/tools/acquire_biometric_models.py; redistribution review is pending,
# so the binaries are never committed.
biometric/internal/biometric-core/models/
biometric/.paddleocr-models/
biometric/.venv/
biometric/.venv-ocr/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.venv/
.DS_Store
